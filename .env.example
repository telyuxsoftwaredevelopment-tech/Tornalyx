# Copiá este archivo a .env y completá con tus credenciales reales.
# NO subas el archivo .env al control de versiones.

DB_HOST=localhost
# Puerto del servidor MySQL. Local suele ser 3306; TiDB Cloud usa 4000.
DB_PORT=3306
DB_NAME=tornalyx_db
DB_USER=tornalyx_dml
DB_PASS=cambiame_por_una_contrasena_fuerte

# ── TLS de la base de datos ───────────────────────────────────────
# Para MySQL gestionado (Aiven, TiDB Cloud, etc.) que exige conexión
# cifrada. En desarrollo local dejalo en 0.
#   DB_SSL=1            activa TLS.
#   DB_SSL_CA           ruta a un bundle de CA, o el certificado PEM
#                       pegado directo (empieza con "-----BEGIN").
#                       Si se deja vacío con DB_SSL=1 se usa el bundle del
#                       sistema (sirve para proveedores con CA pública).
#   DB_SSL_VERIFY=0     desactiva la verificación del cert (no recomendado).
DB_SSL=0
DB_SSL_CA=
DB_SSL_VERIFY=1

# Entorno: development | production
# En production se fuerzan cookies "secure" y otras protecciones.
APP_ENV=development

# ── Envío de correo (2FA por código OTP) ──────────────────────────
# Necesario para el doble factor obligatorio: sin envío válido nadie
# puede iniciar sesión. Hay dos transportes posibles:
#
#  A) API HTTP de Brevo (recomendada en hosting que bloquea SMTP, como
#     Render free). Va por HTTPS. Si BREVO_API_KEY está definida, se usa
#     ESTE transporte y se ignora el SMTP de abajo. Necesitás:
#       - una cuenta gratis en brevo.com y una API key (v3),
#       - verificar el remitente (SMTP_FROM) en el panel de Brevo.
BREVO_API_KEY=

#  B) SMTP puro (sirve en desarrollo local). Para Gmail creá una "App
#     Password" (requiere verificación en 2 pasos en la cuenta de Google).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=app_password_de_16_caracteres

# Remitente (lo usan ambos transportes). Para inbox confiable (sin spam)
# usá un dominio propio verificado en Brevo, no un @gmail.com.
SMTP_FROM=tu_correo@gmail.com
SMTP_FROM_NAME=Tornalyx
